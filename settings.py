# Encoding: UTF-8
"""Settings page.

The page for the user to adjust settings. This requires the use of a database or at 
least management of the included JSON files in the cfg (configuration) directory. This 
page is completely optional and would require refactoring of our existing code to have 
an effect.
"""
